# Rust frontend instructions

The canonical repository and Rust frontend guidance is in [`../AGENTS.md`](../AGENTS.md).
Read that file before modifying code or proofs under `rustfrontend/`; its frontend
architecture, verification notes, build commands, and incremental Rocq workflow
apply here as well. Keep this file as a pointer so the two instruction files do
not drift apart.
