#include <stdio.h>
#include <stdlib.h>
// int f(int x) {
//   return x;
// }
int main() {
  // int  *a = (int *)malloc(5 * sizeof(int));
  // for (int i=0;i<5;i++) {
  //   a[i] = i;
  // }
  // for(int i=0;i<5;i++) {
  //   printf("%d\n", a[i]);
  // }
  // f(4);
  // f(5);
  // int c=2;

  // int b = f(3);
  // printf("%d\n", b);

  int abcd[32];
  // for (int i=0;i<32;i++) {
  //   a[i] = i;
  // }
  // a[3] = 100;
  int* a = &a[0];
  int* b = abcd + 8;
  int* c = abcd + 16;
  int* d = abcd + 24;
  // for (int i=0;i<32;i++) {
  //   printf("%d\n", a[i]);
  // }

  return 0;
}